import {describe, it} from 'mocha';

import {defaultFormatCode} from '../src/index';

describe('js-formatter', () => {
	it('formats JS code according to config', () => {
		// Given
		// Preformatted code
		// When
		const formattedCode = defaultFormatCode('');

		// Then
	});
});
